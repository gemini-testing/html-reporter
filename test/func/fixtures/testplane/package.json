{
  "name": "testplane-fixture-report",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf report",
    "generate": "npx testplane",
    "gui": "npx testplane gui --hostname 0.0.0.0 --port 8001"
  }
}
