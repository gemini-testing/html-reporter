{
  "name": "hermione-fixture-report",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf report",
    "generate": "npx hermione",
    "gui": "npx hermione gui --hostname 0.0.0.0 --port 8001"
  }
}
