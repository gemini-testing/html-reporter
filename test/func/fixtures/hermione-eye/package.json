{
  "name": "hermione-eye",
  "description": "Test project that generates html-report for testing the view in browser (eye button) feature",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf report",
    "generate": "npx hermione",
    "gui": "npx hermione gui --port $(../../utils/get-port.js hermione-eye gui)"
  }
}
