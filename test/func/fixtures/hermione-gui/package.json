{
  "name": "hermione-gui",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf report",
    "generate": "npx hermione --grep 'tests to run'",
    "gui": "npx hermione gui --port $(../../utils/get-port.js hermione-gui gui)"
  }
}
